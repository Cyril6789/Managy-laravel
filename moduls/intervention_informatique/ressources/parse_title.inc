<?php session_start();if(!is_numeric($id_inter)){    if(is_numeric($_GET['id_inter']) AND $_GET['id_inter'] > 0)    {        $id_inter = (int) $_GET['id_inter'];    }    else        $id_inter = 0;    $db->Query('SELECT prefix FROM interventions WHERE id_inter = "'.$id_inter.'" AND compte_principal="'.$_SESSION['compte_principal'].'" ');    $row = $db->Row();    $prefix = $row->prefix;}$page_title = str_replace('%num%', $prefix.$id_inter, $page_title);?>